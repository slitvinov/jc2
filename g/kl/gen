awk '
function norm(x,    s) {
	for (i in x)
		s += x[i]
	for (i in x)
		x[i] /= s	
}

BEGIN {
	s = "1 2 3 4"
	split(s, x)
	s = "5 1.7 2.5 4.8"
	split(s, y)

	norm(x)
	norm(y)

	for (i  in x)
		print x[i], y[i]
}'